---
description: Show open-dynamic-workflows status — active workflows, agents, budget
---
Use the odw_workflows tool to list all workflows known to the local odw daemon (or plugin state if the daemon is offline), then summarize: id, status, phase, agents completed/total, cost so far. If the daemon is not running, say so and show how to install it (from github.com/Suraj1235/open-dynamic-workflows — clone, npm install, npm run setup) and start it (odw-daemon start).
