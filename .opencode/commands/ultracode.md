---
description: Toggle ultracode mode — every substantive task becomes a planned multi-agent workflow
---
Use the odw_ultracode tool to toggle ultracode mode for this project. Report the new state. While ultracode is ON, the open-dynamic-workflows plugin treats every substantive prompt as a workflow trigger: it plans first (task graph → topology → roles → strategy), shows the plan, and orchestrates agents through the odw daemon when available.

$ARGUMENTS
